# 訂單管理系統 (Order Management System)

基於 Payload CMS 3.0 建立的完整訂單管理系統,支援客戶管理、商品管理、訂單管理以及多用戶權限控制。

## 功能特色

- ✨ **客戶管理** - 完整的客戶資料管理,自動生成客戶編號
- 📦 **商品管理** - 商品分類、庫存追蹤、最低庫存警示
- 📝 **訂單管理** - 訂單編號自動生成、金額自動計算、庫存自動扣減
- 👥 **權限控制** - 四種用戶角色(管理員、經理、員工、檢視者)
- 🔄 **自動化流程** - 編號生成、金額計算、庫存管理全自動化

## 系統架構

- **框架**: Payload CMS 3.0 Beta + Next.js 15.0.0
- **資料庫**: MongoDB
- **語言**: TypeScript
- **容器化**: Docker Compose

## 快速開始

### 前置需求

- Node.js >= 20.9.0
- Docker Desktop
- npm 或 pnpm

### 安裝步驟

1. **啟動 MongoDB**
   ```bash
   docker-compose up -d mongo
   ```

2. **安裝依賴**
   ```bash
   npm install
   ```

3. **啟動開發伺服器**
   ```bash
   npm run dev
   ```

4. **訪問管理介面**
   ```
   http://localhost:3000/admin
   ```
   首次訪問會要求建立管理員帳號

## 資料模型

### Users (用戶)
- 支援四種角色:admin, manager, staff, viewer
- 基於角色的權限控制
- 帳號啟用/停用機制

### Customers (客戶)
- 客戶編號自動生成 (CUS-XXXX)
- 完整聯絡資訊
- 建立者追蹤

### Products (商品)
- 商品分類系統
- 庫存管理
- 價格與成本管理
- 圖片上傳

### Orders (訂單)
- 訂單編號自動生成 (ORD-YYYYMMDD-XXXX)
- 多項目訂單支援
- 自動金額計算
- 訂單狀態管理
- 付款狀態追蹤
- 自動庫存扣減/恢復

## 權限說明

| 角色 | Users | Customers | Products | Orders |
|-----|-------|-----------|----------|---------|
| Admin | ✓ 完整 | ✓ 完整 | ✓ 完整 | ✓ 完整 |
| Manager | 僅自己 | ✓ 完整 | ✓ 完整 | ✓ 所有訂單 |
| Staff | 僅自己 | 讀取+建立 | 讀取+更新 | 僅自己的訂單 |
| Viewer | 僅自己 | 僅讀取 | 僅讀取 | 僅讀取 |

## 環境變數

在 `.env` 檔案中設定:

```env
DATABASE_URI=mongodb://127.0.0.1:27017/order-management-system
PAYLOAD_SECRET=your-secret-key-here
```

## 可用指令

```bash
npm run dev          # 啟動開發伺服器
npm run build        # 建置生產版本
npm start            # 啟動生產伺服器
npm run generate:types  # 生成 TypeScript 類型
```

## 專案結構

```
src/
├── collections/
│   ├── Users.ts         # 用戶管理
│   ├── Customers.ts     # 客戶管理
│   ├── Products.ts      # 商品管理
│   ├── Orders.ts        # 訂單管理
│   └── Media.ts         # 媒體檔案
└── payload.config.ts    # Payload 配置
```

## 業務流程

1. **建立客戶** → 系統自動生成客戶編號
2. **建立商品** → 設定價格和庫存
3. **建立訂單** → 選擇客戶和商品
4. **自動計算** → 系統自動計算總金額
5. **確認訂單** → 自動扣減商品庫存
6. **取消訂單** → 自動恢復商品庫存

## 開發者

本系統使用 Payload CMS 3.0 Beta 開發,為學習和展示用途。

## 注意事項

⚠️ **Beta 版本**: Payload 3.0 目前處於 Beta 階段,可能存在未知問題。建議在生產環境使用穩定版本。

## 授權

MIT License
